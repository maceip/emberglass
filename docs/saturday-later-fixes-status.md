# Saturday review.MD — Later Fixes Status

From the "Later Fixes" section. These are to be tackled as **real capabilities**, never local substitutes.

- [ ] Rebuild benchmark reporting from real browser runs against real model weights, with raw artifacts committed alongside any public number.
- [ ] Rebuild F-16 verification only as a real-model parity/performance check.
- [ ] Design the app-action layer before implementation, including provider auth, write idempotency, read-after-write verification, and cleanup.
- [ ] Design the Chrome extension / side-panel architecture before adding extension code.
- [ ] Rebuild provider execution tests against dedicated real test accounts, not local stores.
- [ ] Rework training/eval provenance so user-facing claims distinguish generated teaching examples from real user/test-account eval evidence.
- [ ] Do a separate UI/UX reset against the approved skillbook, skill screen, and job-board direction.

Current date in this workspace: 2026-06-27 (per conversation).

All items remain open. Work on them only when the relevant Recovery Work Card gates are passed and only with real evidence.
