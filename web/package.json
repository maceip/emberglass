{
  "name": "emberglass-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Emberglass — Threlte single-page showcase for in-browser WebGPU LoRA: demo, train, tune, inference, with bespoke 3D loading animations.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.19.0",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@threlte/core": "^8.0.2",
    "@threlte/extras": "^9.1.0",
    "three": "^0.171.0"
  }
}
